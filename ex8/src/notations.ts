// Notations ( Tipagens relacionadas a paramêtros e retornos de função )

const soma = (a: number, b: number): number => a + b;
soma(1, 2);

//Retorno vazio é chamado de VOID

const noReturn = (menssagem: string) => {
  console.log(menssagem);
};
